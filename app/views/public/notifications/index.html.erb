<div class="container">
  <div class="row p-4">
    <h4>
      <strong>通知</strong>
    </h4>
  </div>
  <div class="row">
    <% if @notifications.exists? %>
      <div class="users-index">
        <%= render @notifications %>
        <%= paginate @notifications %>
      </div>
    <% else %>
      <p>通知はありません</p>
    <% end %>
  </div>
</div>